import{query}from"../../lib/db.js";import{auth,admin}from"../../lib/auth.js";import{corsMw,ok}from"../../lib/http.js";export default async(req,res)=>{corsMw(req,res,async()=>{if(!ok(req,res,["GET","POST"]))return;if(req.method==="GET"){const r=await query("SELECT * FROM banners WHERE active=true ORDER BY created_at DESC");return res.json({banners:r.rows})}auth(req,res,()=>admin(req,res,async()=>{const b=req.body||{};const r=await query("INSERT INTO banners(title,description,image_url,cloudinary_public_id,button_text,button_link,active) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING *",[b.title,b.description||"",b.image_url,b.cloudinary_public_id||null,b.button_text||"SHOP NOW",b.button_link||"/categories",b.active!==false]);res.status(201).json({banner:r.rows[0]})}))})};
