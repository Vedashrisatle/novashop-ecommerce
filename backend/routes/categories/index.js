import{query}from"../../lib/db.js";import{auth,admin}from"../../lib/auth.js";import{corsMw,ok}from"../../lib/http.js";export default async(req,res)=>{corsMw(req,res,async()=>{if(!ok(req,res,["GET","POST"]))return;if(req.method==="GET"){const r=await query("SELECT * FROM categories ORDER BY name");return res.json({categories:r.rows})}auth(req,res,()=>admin(req,res,async()=>{const{name,image_url}=req.body||{};const slug=name.toLowerCase().replace(/[^a-z0-9]+/g,"-");const r=await query("INSERT INTO categories(name,slug,image_url) VALUES($1,$2,$3) RETURNING *",[name,slug,image_url||null]);res.status(201).json({category:r.rows[0]})}))})};
