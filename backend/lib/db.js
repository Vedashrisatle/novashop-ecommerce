import pg from"pg";const{Pool}=pg;export const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false},max:5});export const query=(s,p)=>pool.query(s,p);export const client=()=>pool.connect();
