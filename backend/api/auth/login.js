import bcrypt from"bcryptjs";import{query}from"../../lib/db.js";import{token,safe}from"../../lib/auth.js";import{corsMw,ok}from"../../lib/http.js";
export default async(req,res)=>{corsMw(req,res,async()=>{if(!ok(req,res,["POST"]))return;try{const{email,password}=req.body||{};const r=await query("SELECT * FROM users WHERE email=$1",[String(email||"").toLowerCase()]);if(!r.rowCount||!(await bcrypt.compare(password||"",r.rows[0].password_hash)))return res.status(401).json({message:"Invalid email or password"});res.json({token:token(r.rows[0]),user:safe(r.rows[0])})}catch{res.status(500).json({message:"Unable to login"})}})}
