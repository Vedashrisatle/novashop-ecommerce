import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{endpoints}from"../services/api";
export default function AdminProducts(){const[p,setP]=useState([]);const load=()=>endpoints.products({limit:100}).then(r=>setP(r.data.products||[]));useEffect(load,[]);async function del(id){if(confirm("Delete product?")){await endpoints.deleteProduct(id);load()}}return <><h1>Products</h1><Link className="btn" to="/admin/products/new">Add</Link><div className="table"><table><thead><tr><th>Image</th><th>Name</th><th>Price</th><th>Stock</th><th>Action</th></tr></thead><tbody>{p.map(x=><tr key={x.id}><td><img className="tiny" src={x.image_url} alt={x.name}/></td><td>{x.name}</td><td>₹{x.price}</td><td>{x.stock}</td><td><Link to={`/admin/products/${x.id}`}>Edit</Link> <button onClick={()=>del(x.id)}>Delete</button></td></tr>)}</tbody></table></div></>}
