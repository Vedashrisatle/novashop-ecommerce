import{useEffect,useState}from"react";import{endpoints}from"../services/api";const S=["pending","confirmed","processing","shipped","delivered","cancelled"];export default function AdminOrders(){const[o,setO]=useState([]);const load=()=>endpoints.orders().then(r=>setO(r.data.orders||[]));useEffect(load,[]);async function c(id,status){await endpoints.updateOrder(id,{status});load()}return <><h1>Orders</h1><div className="table"><table><thead><tr><th>ID</th><th>Customer</th><th>Total</th><th>Payment</th><th>Status</th></tr></thead><tbody>{o.map(x=><tr key={x.id}><td>#{x.id}</td><td>{x.customer_name||x.user_id}</td><td>₹{x.total_amount}</td><td>{x.payment_status}</td><td><select value={x.order_status} onChange={e=>c(x.id,e.target.value)}>{S.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div></>}
