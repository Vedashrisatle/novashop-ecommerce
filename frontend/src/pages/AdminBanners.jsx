import{useEffect,useState}from"react";import{endpoints}from"../services/api";export default function AdminBanners(){const[b,setB]=useState([]);const[f,setF]=useState({title:"",description:"",image_url:"",cloudinary_public_id:"",button_text:"SHOP NOW",button_link:"/categories",active:true});const[file,setFile]=useState();const load=()=>endpoints.banners().then(r=>setB(r.data.banners||[]));useEffect(load,[]);async function up(){const fd=new FormData();fd.append("file",file);fd.append("folder","banners");const r=await endpoints.upload(fd);setF({...f,image_url:r.data.secure_url,cloudinary_public_id:r.data.public_id})}async function save(e){e.preventDefault();await endpoints.createBanner(f);setF({...f,title:"",description:"",image_url:"",cloudinary_public_id:""});load()}async function del(id){if(confirm("Delete banner?")){await endpoints.deleteBanner(id);load()}}return <><h1>Banners</h1><form className="form" onSubmit={save}><input placeholder="Title" required value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><textarea placeholder="Description" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><input type="file" accept="image/*" onChange={e=>setFile(e.target.files[0])}/><button type="button" className="btn outline" onClick={up} disabled={!file}>Upload</button><input required placeholder="Cloudinary URL" value={f.image_url} onChange={e=>setF({...f,image_url:e.target.value})}/><button className="btn">Add banner</button></form>{b.map(x=><div className="banner" key={x.id}><img src={x.image_url} alt={x.title}/><b>{x.title}</b><button onClick={()=>del(x.id)}>Delete</button></div>)}</>}
