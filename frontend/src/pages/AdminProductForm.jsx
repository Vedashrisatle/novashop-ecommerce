import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import{endpoints}from"../services/api";
export default function AdminProductForm(){const{id}=useParams();const nav=useNavigate();const[f,setF]=useState({name:"",description:"",price:"",original_price:"",discount:0,category_id:"",brand:"",stock:0,rating:5,image_url:"",cloudinary_public_id:""});const[c,setC]=useState([]);const[file,setFile]=useState();useEffect(()=>{endpoints.categories().then(r=>setC(r.data.categories||[]));if(id)endpoints.product(id).then(r=>setF(r.data.product))},[id]);async function upload(){const fd=new FormData();fd.append("file",file);fd.append("folder","products");const r=await endpoints.upload(fd);setF({...f,image_url:r.data.secure_url,cloudinary_public_id:r.data.public_id})}async function save(e){e.preventDefault();const d={...f,price:+f.price,original_price:+(f.original_price||f.price),discount:+f.discount,category_id:+f.category_id,stock:+f.stock,rating:+f.rating};if(id)await endpoints.updateProduct(id,d);else await endpoints.createProduct(d);nav("/admin/products")}return <><h1>{id?"Edit":"Add"} product</h1><form className="form" onSubmit={save}>{["name","description","price","original_price","discount","brand","stock","rating"].map(k=><label key={k}>{k}<input required={k!=="discount"} value={f[k]??""} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<label>Category<select required value={f.category_id} onChange={e=>setF({...f,category_id:e.target.value})}><option value="">Select</option>{c.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Image<input type="file" accept="image/*" onChange={e=>setFile(e.target.files[0])}/></label><button type="button" className="btn outline" disabled={!file} onClick={upload}>Upload to Cloudinary</button>{f.image_url&&<img className="preview" src={f.image_url} alt="Product preview"/>}<input required value={f.image_url} onChange={e=>setF({...f,image_url:e.target.value})} placeholder="Cloudinary URL"/><button className="btn">Save</button></form></>}
