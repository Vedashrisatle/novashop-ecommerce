import{useEffect,useState}from"react";import{useParams}from"react-router-dom";import{endpoints}from"../services/api";import ProductCard from"../components/ProductCard";import{Loading,Empty}from"../components/States";
export default function Category(){const{slug}=useParams();const[p,setP]=useState([]);const[q,setQ]=useState("");const[sort,setS]=useState("");useEffect(()=>{endpoints.products({category:slug,search:q,sort,limit:40}).then(r=>setP(r.data.products||[]))},[slug,q,sort]);return <section className="section"><div className="head"><div><p>COLLECTION</p><h1>{slug.replaceAll("-"," ")}</h1></div><div><input placeholder="Search category" value={q} onChange={e=>setQ(e.target.value)}/><select value={sort} onChange={e=>setS(e.target.value)}><option value="">Sort</option><option value="price_asc">Price low-high</option><option value="price_desc">Price high-low</option><option value="rating">Top rated</option></select></div></div>{p.length?<div className="grid">{p.map(x=><ProductCard key={x.id} p={x}/>)}</div>:<Empty>No products found.</Empty>}</section>}
